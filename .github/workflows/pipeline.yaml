name: CICD pipeline
on: 
    push:
      paths:
       - 'src/**'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Show repository
        run: echo "Repository is ${{ github.ref }}"