name: CICD pipeline
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ./build.sh

  notify:
    needs: build
    if: ${{ always() }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build failed"